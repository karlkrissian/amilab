//
// C++ Implementation: LineCC
//
// Description: 
//
//
// Author: Karl Krissian,,, <karl@UBUNTU-KARL>, (C) 2008
//
// Copyright: See COPYING file that comes with this distribution
//
//

#include "LineCC.h"
#include <iostream>
#include <vector>
#include <stack>
using namespace std;
using namespace amilab;

SurfacePoly* GetConnectedComponent( SurfacePoly* input, int line_id)
{

/* TODO: finish
  surf = new SurfacePoly;
  surf->NewSurface();

  // check that the line exists
  if ((line_id<0)||(line_id>=input->GetNumberOfLines())) {
     cerr << "GetConnectedComponent() \t bad line number " << endl;
  }

  // create a array that marks the lines already visited
  // initialized to false
  vector<bool> visited_line(input->GetNumberOfLines(),false);

  // create a stack of lines to process
  stack<int> lines_to_process;
  list<int> connected_lines;
  lines_to_process.push(line_id)

  // create a list of connected lines
  while (!lines_to_process.empty()) {
    int line = lines_to_process.pop();
    visited_line[line] = true;
    
  }
*/
  return NULL;
} // GetConnectedComponent()

