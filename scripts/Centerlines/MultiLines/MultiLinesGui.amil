
func "Centerlines/MultiLines/MultiLinesClass"

#----------------------------
# Main
#----------------------------

if (!exists(MultiLines_obj)) {
  MultiLinesClass MultiLines_obj
  MultiLines_obj.Gui()
  
} else {
  InfoDialog "The script seems to be already loaded." 
}

