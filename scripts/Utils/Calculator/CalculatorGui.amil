
func "CalculatorClass"

#----------------------------
# Main
#----------------------------

if (!exists(Calc_obj)) {

  CalculatorClass Calc_obj
  Calc_obj.Gui()
  

} else {
  InfoDialog "The script seems to be already loaded."
}
