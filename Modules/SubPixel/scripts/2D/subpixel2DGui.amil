
func "subpixel2DClass.amil"

#----------------
# Main
#----------------
if (!exists(subp2D)) {
  Subpixel2DClass subp2D
  subp2D.CreateGui()
} else {
  InfoDialog "The script for subpixel edge detection in 2D images seems to be already loaded."
}
