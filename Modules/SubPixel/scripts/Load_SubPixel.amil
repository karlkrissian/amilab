
func "Common/ModulePlugin"

if (!exists(SubPixel_obj)) {
  ModulePlugin SubPixel_obj
  SubPixel_obj.SetModuleName("SubPixel")
  SubPixel_obj.Load()
} else {
  InfoDialog "The script object SubPixel_obj to be already instanciated."
}

